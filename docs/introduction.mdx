---
title: "Introduction"
description: "Example-first overview of Hile packages and AI-facing usage rules."
---

{/* Generated by scripts/build-ai-context.mjs from docs/ai. Do not edit by hand. */}

# Hile

Hile is a lightweight Node.js service toolkit for HTTP APIs, Next.js apps, reusable models, message-based microservices, jobs, and Redis-backed reliability primitives.

The docs in this repository are generated from the same AI context that powers `skills/hile/`, `llms.txt`, `llms-full.txt`, and every package `AI.md`.

## Core Mental Model

- `@hile/core` manages resources as async services. A boot file starts things; `shutdown()` callbacks release them.
- `@hile/http` exposes Koa routes and file-system controllers.
- `@hile/model` is the reusable business-logic layer. Controllers and Next.js pages should call models instead of duplicating domain logic.
- `@hile/micro` uses message loaders and WebSocket clients for service discovery and RPC.
- Redis primitives (`redis-lock`, `redis-idempotency`, `redis-rate-limit`, `redis-stream-queue`, `cache`) are distributed coordination helpers, not exactly-once systems.

## Package Selection Matrix

| User asks for | Use | Also read |
|---|---|---|
| Start an app, manage lifecycle, graceful shutdown | `@hile/core`, `@hile/cli`, `@hile/bootstrap` | [Core lifecycle](/packages/core) |
| Create an HTTP endpoint or Koa middleware | `@hile/http` | [HTTP API](/packages/http), [HTTP, Model, and TypeORM](/techniques/database) |
| Run Next.js and API controllers on one port | `@hile/http-next` | [HttpNext](/packages/http-next), [HttpNext fullstack](/fundamentals/http-next-pipeline) |
| Run independently built RSC plugins through one Host | `@hile/rsc` | [RSC packages](/packages/rsc), [Complete RSC plugin guide](/techniques/rsc-plugins) |
| Compile immutable production RSC plugin artifacts | `@hile/rsc-build` | [RSC packages](/packages/rsc) |
| Add incremental RSC development and hot reload | `@hile/rsc-development` | [RSC packages](/packages/rsc) |
| Decode plugin Flight inside a Next Host | `@hile/rsc-next` | [RSC packages](/packages/rsc), [Complete RSC plugin guide](/techniques/rsc-plugins) |
| Put business logic somewhere reusable | `@hile/model` | [Model and context](/packages/model) |
| Propagate request id, tenant id, logger bindings, or micro context | `@hile/context` | [Model and context](/packages/model) |
| Connect to SQL through TypeORM | `@hile/typeorm` | [Core lifecycle](/packages/core), [Infrastructure architecture](/architecture) |
| Connect to Redis | `@hile/ioredis` | [Core lifecycle](/packages/core), [Infrastructure architecture](/architecture) |
| Create structured logs | `@hile/logger` | [Core lifecycle](/packages/core), [Infrastructure architecture](/architecture) |
| Add read-through Redis cache | `@hile/cache` | [Infrastructure architecture](/architecture), [Redis cache](/techniques/caching) |
| Run cron or delayed jobs | `@hile/schedule` | [Infrastructure architecture](/architecture) |
| Build a custom file loader or understand route mapping | `@hile/loader` | [Infrastructure architecture](/architecture) |
| Add request/response messaging over WS/IPC/worker | `@hile/message-*` | [Messaging and Micro](/packages/micro) |
| Load file-based message handlers | `@hile/message-loader` | [Messaging and Micro](/packages/micro), [Micro RPC](/techniques/messaging) |
| Build service discovery or RPC | `@hile/micro` | [Messaging and Micro](/packages/micro), [Micro RPC](/techniques/messaging) |
| Expose distributed microservice capabilities through one MCP server | `@hile/mcp` | [MCP API](/packages/mcp), [Distributed MCP gateway](/techniques/mcp-gateway) |
| Push runtime config without restarts | `@hile/micro-dynamic-configs` | [Messaging and Micro](/packages/micro), [Runtime config](/techniques/configuration) |
| Stabilize config-driven runtime reloads | `@hile/reloader` | [Reloader](/packages/reloader), [Stable runtime reload](/packages/reloader) |
| Compose topics, config, and reloader state as refs | `@hile/reactivity` | [Reactivity](/packages/reactivity), [Stable runtime reload](/packages/reloader) |
| Protect a critical section with a lease | `@hile/redis-lock` | [Redis reliability](/techniques/idempotency) |
| Stop duplicate retry side effects | `@hile/redis-idempotency` | [Redis reliability](/techniques/idempotency), [Queue idempotency](/techniques/idempotency) |
| Enforce shared quotas or 429s | `@hile/redis-rate-limit` | [Redis reliability](/techniques/idempotency) |
| Persist and retry background jobs | `@hile/redis-stream-queue` | [Redis reliability](/techniques/idempotency), [Queue idempotency](/techniques/idempotency) |
| Scaffold a new project | `create-hile` | [create-hile](/packages/create-hile), [Project scaffold](/quickstart) |

## Common Selection Rules

- If the task is a user-facing API, start with `@hile/http` or `@hile/http-next`.
- If the task is reusable business behavior, put it in `@hile/model` even if the caller is HTTP, Next.js, queue, or micro.
- If the task may retry or redeliver and has side effects, add `@hile/redis-idempotency`.
- If the task is background work, use `@hile/redis-stream-queue`; do not use micro RPC as a queue.
- If the task is "only one replica should run this", use `@hile/redis-lock` or `@hile/schedule` distributed mode, and keep a final business consistency wall.
- If the task is "prevent too many attempts", use `@hile/redis-rate-limit`; do not confuse it with authentication or idempotency.
- If config changes can arrive in bursts, use `@hile/reloader` around the runtime; do not reload directly inside every subscribe callback.
- If service code needs refs or async watch strategies for topics/config/reloader state, use `@hile/reactivity`.
- If tools, resources, and prompts belong to different microservices, use `@hile/mcp`; do not centralize their handlers in the gateway.

## Start With Examples

For implementation tasks, pick the package from the matrix and copy the closest recipe before writing custom abstractions.

- [HTTP API + model + TypeORM](/techniques/database)
- [HttpNext fullstack app](/fundamentals/http-next-pipeline)
- [Single-listener RSC plugin platform](/techniques/rsc-plugins)
- [Redis cache with singleflight](/techniques/caching)
- [Queue worker with idempotency](/techniques/idempotency)
- [Micro RPC with message loader](/techniques/messaging)
- [Distributed MCP providers and gateway](/techniques/mcp-gateway)
- [Runtime dynamic config](/techniques/configuration)
- [New scaffolded project](/quickstart)
