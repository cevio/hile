# ADR-0003: @hile/cli 接入容器事件日志

## 背景

CLI 负责应用启动编排，但此前缺少统一的服务启动/失败/关闭阶段日志，排障成本较高。

## 决策

CLI 启动时订阅 `container.onEvent`，输出关键事件日志；退出时取消订阅并执行 `container.shutdown()`。

## 影响

正向：

- 启动耗时、失败原因、关闭阶段对外可见
- 线上问题定位更快

代价：

- 默认日志量增加
- 需保持日志格式稳定

## 备选方案

1. 仅打印“启动成功/失败”：信息粒度不足
2. 业务服务各自日志：格式与语义不统一
