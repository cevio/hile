{
  "name": "hile-monorepo",
  "private": true,
  "type": "module",
  "homepage": "https://pulian.mintlify.app",
  "repository": {
    "type": "git",
    "url": "https://github.com/cevio/hile.git"
  },
  "author": "cevio",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/cevio/hile/issues"
  },
  "scripts": {
    "build": "lerna run build",
    "test": "lerna run test",
    "test:coverage": "lerna run test -- --coverage && node scripts/coverage-summary.mjs",
    "dev": "lerna run dev",
    "publish": "lerna publish",
    "build:ai-context": "node scripts/build-ai-context.mjs",
    "create": "bash scripts/create-package.sh",
    "update-template-hile-deps": "node scripts/update-template-hile-deps.mjs",
    "update-template-hile-deps:dry-run": "node scripts/update-template-hile-deps.mjs --dry-run"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.0.18",
    "lerna": "^8.2.2",
    "typescript": "^5.9.3"
  },
  "workspaces": [
    "packages/*"
  ]
}
