# lerna version/publish 会在背后起 `pnpm install --lockfile-only` 子进程；
# 当 node_modules 状态与期望不一致时，pnpm 会弹“是否删除重建 node_modules? (Y/n)”交互提示，
# 而该提示被子进程管道吞掉，表现为无提示的死等（卡住）。关闭此确认即可让 pnpm 自动放行，不再静默卡死。
confirm-modules-purge=false